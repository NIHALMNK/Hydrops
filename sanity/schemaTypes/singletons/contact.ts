import { defineField, defineType } from 'sanity'; import { MobileDeviceIcon } from '@sanity/icons/MobileDevice';  export const contact = defineType({   name: 'contact',   title: 'Contact Information',   type: 'document',   icon: MobileDeviceIcon,   description: 'Global contact details (Phone, WhatsApp, Email)',   fields: [     defineField({       name: 'contactInfo',       title: 'Contact Details',       type: 'contactInfo',       description: 'The primary ways to reach the business',       validation: (Rule) => Rule.required(),     }),   ],   preview: {     prepare() {       return {         title: 'Contact Information',         icon: MobileDeviceIcon,       };     },   }, });
