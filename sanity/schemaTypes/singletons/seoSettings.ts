import { defineField, defineType } from 'sanity'; import { SearchIcon } from '@sanity/icons/Search';  export const seoSettings = defineType({   name: 'seoSettings',   title: 'SEO Defaults',   type: 'document',   icon: SearchIcon,   description: 'Global search engine optimization defaults',   fields: [     defineField({       name: 'seo',       title: 'Default SEO',       type: 'seo',       description: 'These settings will be used as fallbacks if a page does not define its own SEO',       validation: (Rule) => Rule.required(),     }),   ],   preview: {     prepare() {       return {         title: 'SEO Defaults',         icon: SearchIcon,       };     },   }, });
