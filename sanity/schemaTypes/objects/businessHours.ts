import { defineField, defineType } from 'sanity'; import { ClockIcon } from '@sanity/icons/Clock';  export const businessHours = defineType({   name: 'businessHours',   title: 'Business Hours',   type: 'object',   icon: ClockIcon,   description: 'Opening and closing times for the business',   fields: [     defineField({       name: 'weekdays',       title: 'Weekdays Label',       type: 'string',       description: 'Label for the open days (e.g., "Mon - Sat")',       validation: (Rule) => Rule.required(),     }),     defineField({       name: 'hours',       title: 'Hours',       type: 'string',       description: 'The operating hours (e.g., "09:00 AM — 06:00 PM")',       validation: (Rule) => Rule.required(),     }),     defineField({       name: 'closedDay',       title: 'Closed Day Label',       type: 'string',       description: 'Label for the closed days (e.g., "Sunday")',       validation: (Rule) => Rule.required(),     }),     defineField({       name: 'closedLabel',       title: 'Closed Status Label',       type: 'string',       description: 'Text indicating the business is closed (e.g., "Closed")',       validation: (Rule) => Rule.required(),     }),   ],   preview: {     select: {       title: 'weekdays',       subtitle: 'hours',     },     prepare({ title, subtitle }) {       return {         title: title ? `${title} Hours` : 'Business Hours',         subtitle: subtitle || 'No hours provided',         icon: ClockIcon,       };     },   }, });
