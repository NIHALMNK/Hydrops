import { defineField, defineType } from 'sanity'; import { SearchIcon } from '@sanity/icons/Search';  export const seo = defineType({   name: 'seo',   title: 'SEO Settings',   type: 'object',   icon: SearchIcon,   description: 'Search Engine Optimization metadata',   fields: [     defineField({       name: 'metaTitle',       title: 'Meta Title',       type: 'string',       description: 'Title used for search engines and browser tabs (max 60 characters)',       validation: (Rule) => Rule.max(60).warning('Longer titles may be truncated by search engines.'),     }),     defineField({       name: 'metaDescription',       title: 'Meta Description',       type: 'text',       description: 'Description used for search engine results (max 160 characters)',       validation: (Rule) => Rule.max(160).warning('Longer descriptions may be truncated by search engines.'),     }),     defineField({       name: 'keywords',       title: 'Keywords',       type: 'array',       description: 'Keywords relevant to this page',       of: [{ type: 'string' }],     }),     defineField({       name: 'openGraphImage',       title: 'Open Graph Image',       type: 'cloudinaryImage',       description: 'Image displayed when sharing the link on social media',     }),   ], });
