'use client';

/**
 * Note: SoulStatement expanding arch reveal is now natively unified directly
 * inside the Master Hero Pinned Viewport (HeroScene.tsx) for 0 white gaps,
 * 0 layout distance, and 100% frozen frame overlapping.
 */
export function SoulStatement() {
  return null;
}
